apiVersion: apps/v1
kind: Deployment
metadata:
  name: asp-product-feedback-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: asp-product-feedback-server
  template:
    metadata:
      labels:
        app: asp-product-feedback-server
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: "kubernetes.io/hostname"
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: asp-product-feedback-server
      containers:
        - name: server
          image: alexandermatveev2908/asp-product-feedback-server:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: asp-product-feedback
