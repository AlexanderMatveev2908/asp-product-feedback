apiVersion: v1
kind: Service
metadata:
  name: asp-product-feedback-client
spec:
  type: NodePort
  selector:
    app: asp-product-feedback-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
      nodePort: 30081
