apiVersion: apps/v1
kind: Deployment
metadata:
  name: asp-product-feedback-client
spec:
  replicas: 2
  selector:
    matchLabels:
      app: asp-product-feedback-client
  template:
    metadata:
      labels:
        app: asp-product-feedback-client
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: 'kubernetes.io/hostname'
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: asp-product-feedback-client
      containers:
        - name: client
          image: alexandermatveev2908/asp-product-feedback-client:latest
          ports:
            - containerPort: 3001
          envFrom:
            - secretRef:
                name: pfn-job-application-tracker
